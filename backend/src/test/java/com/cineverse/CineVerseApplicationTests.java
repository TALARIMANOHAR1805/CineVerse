package com.cineverse;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Smoke test — verifies that the Spring application context loads
 * without errors. If any bean is misconfigured, this test fails.
 */
@SpringBootTest
class CineVerseApplicationTests {

    @Test
    void contextLoads() {
        // Passes if the application context starts successfully
    }
}
